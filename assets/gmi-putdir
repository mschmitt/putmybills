#!/usr/bin/env bash

while read -r file
do
	if gmi-upload --file "${file}"
	then
		if [[ -d "${1}/uploaded" ]]
		then
			mv "${file}" "${1}/uploaded"
		fi
	fi
done < <(find "${1}" -maxdepth 1 -type f -iname '*.PDF')
